<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kn.beautynow">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/new_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/new_icon"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.usuario.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.usuario.Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.usuario.Cadastro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.cliente.ClienteMenu"
            android:label="@string/title_activity_cliente_menu"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".gui.fornecedor.FornecedorMenu"
            android:label="@string/title_activity_fornecedor_menu"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>